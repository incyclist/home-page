User-agent: *
Sitemap: https://incyclist.com/sitemap.xml

# Nothing to hide: the pages are hand-written HTML and every asset they load
# should stay crawlable so search engines can render them.
Disallow:
